se hace instalasion del Esqueleto del propyecto el cual tiene react+tailwind.

se mejora la extructura y se elimina codigo predeterminado del esqueleto el cual no servia para mi pagina.

se crean carpetas como layout y su archivo rootLayout antes estaba el MainLayout pero este fue cambiado debiado a que era 
arvhico mas estandar al que requeria ya que este es para barios layout y para usuarios normal a diferencia del rootLayout que sirve 
para el layout raiz este envuelve toda la app archivos mas globales como navbar, footer.

se instala el Routter y se configura en app.jsx en la importancion de app.jsx escribo la siguiente linea  BrowserRouter
esta ayuda a sincronizar la interfaz de usuario con la URL navegador permitiendo la navegación 
entre componentes sin recargar la página completa.

se crea el documento Navbar para la barra superior de mi pagina web esto manejaria el menu principal, acceso a rutas,
punto de navegacion en general

Se utiliza <Link to="/register" porque el Link nos sirve para navegar entre paginas sin que esta tenga que recargar toda la web

se a instalado y se creo to la estructura con supabase para el control de la cuentas y seguridad de la pagina ahora esta "funcionando"

Se crea una carpeta llamada context la cual nos va ayudar a tener una mejor organizacion con respecto a nuestra protected Routes que nos va a servir para que solo 
una persona que haya creado una cuenta pueda ver los productos que se manejan en nuestra tienda y a esto le vamos a implementar un listener que va de la mano con los antes 
ya mencionados a validar que pasa con la Cuenta de X usuario si la sesion expira que lo saque de los productos o si inicia sesion en otra pagina o navegador

tambien editamos main.jsx para envolver toda la pagina en el authcontext para las validaciones ya mensionadas de las cuentas que sean creadas 
se hace nueva configuracion en Navbar ya que la estructura anterior estaba mal debido a la implementacion de supabase y de tener const { data: listener } = supabase.auth.onAuthStateChange((_event, session) => {
    setSession(currentSession)
    setLoading(false) esto paso a tener const {
  data: { subscription },
} = supabase.auth.onAuthStateChange((_event, session) => {
  setSession(session);
  setLoading(false);
});

return () => {
  subscription.unsubscribe();
}; esto hace que cuando una cuenta engrese esto hace el efecto de limpiar cada sesion abierta o cerrada haciendo que no haya bugs como elq ue tenia de
no poder entrar a la pagina de productos cuando iniciaba sesion

Se crea un archivo llamado hero.jsx este nos ayuda a darle mejor visibilidad a nuestra pagina visualmente a que sea mas llamativa  